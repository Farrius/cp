#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using ld = long double;

int main () {
    string s;
    cin >> s;
    string t = "Hello,World!";
    if ((int) s.size() != (int) t.size() || t != s) cout << "WA" << '\n';
    else cout << "AC" << '\n';
}
