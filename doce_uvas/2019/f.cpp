#include <bits/stdc++.h>

using namespace std;
using ll = long long;
using ld = long double;

const int MX = 100;

pair<int, int> ar[MX];
int w[MX];

int main () {
	int t;
	cin >> t;
	while (t--) {
		cin >> n;
		for (int i = 0; i < n; i++) {
			cin >> ar[i].first >> ar[i].second;
			cin >> w[i];
		}
		int sol = 0;
		for (int i = 0; i < n; i++) {
			for (int j = i + 1; j < n; j++) {
				i	
			}
		}
		cout << sol << '\n';
	}
}
