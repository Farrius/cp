#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main () {
	int t;
	cin >> t;
	for (int test = 1; test <= t; test++) {
		int n, w;
		cin >> n >> w;
		int ar[n];
		for (int i = 0; i < n; i++) {
			cin >> ar[i];
		}
		sort(ar, ar + n);

		ll sol = 1e18, tot = 0;
	}
}
